language = "php"

run = [
    "php", "artisan", "serve",
    "--host", "0.0.0.0",
    "--port", "8000"
]

entrypoint = "routes/web.php"

[packager]
language = "php"

[nix]
channel = "stable-22_11"

[[ports]]
localPort = 8000
externalPort = 80
